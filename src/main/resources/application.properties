# Hibernate config
# Hibernate ddl auto (create, create-drop, update, validate, none): with "update" the database schema will be automatically updated accordingly to java entities found in the project
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true
spring.jpa.database=MYSQL

# Show or not log for each sql query
spring.jpa.show-sql=true

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.hibernate.dialect=com.sheennae.serious.configs.LocalMysqlDialect
spring.jpa.properties.hibernate.format_sql=true
spring.datasource.validation-query=SELECT 1
spring.datasource.test-on-borrow=false
spring.datasource.test-on-return=false
spring.datasource.sql-script-encoding= UTF-8

#JDBC Connection url for the database
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://serious-db.cemg0s34i4lr.ap-northeast-2.rds.amazonaws.com:3306/serious_db?useUnicode=true&charaterEncoding=utf-8&useSSL=false
spring.datasource.username=serious
spring.datasource.password=mashupserious132

# message
howling.hello.message=[develop mode] Hello libqa.com!

# view Resolver cache config
environment.viewResolver.cached=false

# log level config
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO

